/**
 * Per-game theme seam. See `relativism/theme.ts` — all games currently share
 * one Panda token set; diverge under `[data-theme="tigers-path"]` when needed.
 */
export const TIGERS_PATH_THEME_ID = "tigers-path"
