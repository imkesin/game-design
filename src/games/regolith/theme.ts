/**
 * Per-game theme seam. See `relativism/theme.ts` — all games currently share
 * one Panda token set; diverge under `[data-theme="regolith"]` when needed.
 */
export const REGOLITH_THEME_ID = "regolith"
